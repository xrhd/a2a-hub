[project]
name = "commons"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
hello = "commons:hello"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
